# Exemple d'environnement pour le déploiement sur Railway / PaaS
# Copiez ce fichier en `.env` localement pour le développement si besoin.

# Sécurité
DJANGO_SECRET_KEY=replace-with-a-secure-secret
DJANGO_DEBUG=0  # 0 pour production, 1 pour debug local
DJANGO_ALLOWED_HOSTS=your-app.up.railway.app www.example.com

# Database (Railway fournit DATABASE_URL automatiquement après ajout de Postgres)
# Exemple: postgres://user:password@host:port/dbname
DATABASE_URL=postgres://user:password@host:port/dbname

# Email (optionnel)
DJANGO_EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
DJANGO_DEFAULT_FROM_EMAIL=noreply@example.com

# Static/media (optionnel) - pour utiliser S3
# AWS_STORAGE_BUCKET_NAME=your-bucket
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_S3_REGION_NAME=...
# AWS_S3_CUSTOM_DOMAIN=...

# Security options (optionnel)
# SESSION_COOKIE_SECURE=1
# CSRF_COOKIE_SECURE=1
# SECURE_SSL_REDIRECT=1
# SECURE_HSTS_SECONDS=31536000
# SECURE_HSTS_INCLUDE_SUBDOMAINS=1
# SECURE_HSTS_PRELOAD=1
